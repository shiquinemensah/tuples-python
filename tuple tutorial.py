coordinates = (4,5),(6,5),(4,3),6)
# Tuple is information that connot be changed, onece you have assigned the
# Information you cannot change it with a different set oo code.
# Tuples are used to create important information that cannot be changed.
# the different between a tuple and a string is that a string can be changed but qa tuple cannot.
print (coordinates[0])
# always use print to start an action or event

